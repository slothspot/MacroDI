# Bootstrap config for test purposes

default {
    engine = name.dmitrym.DefaultEngine
}

prod {
    engine = name.dmitrym.ProdEngine
}

test {
    engine = name.dmitrym.TestEngine
}

broken {
    engine = DoesNotExistEngine
}

invalid {
    engne = default.engine
}

